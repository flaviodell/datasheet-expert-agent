services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - chroma_data:/app/chroma_db
      - uploads_data:/app/uploads
      - cache_data:/app/parsed_cache

volumes:
  chroma_data:
  uploads_data:
  cache_data: